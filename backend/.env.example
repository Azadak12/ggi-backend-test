PORT=3000

DB_HOST=localhost
DB_PORT=5434
DB_USERNAME=ggi
DB_PASSWORD=ggi
DB_NAME=ggi_backend_test

# Comma separated origins allowed to call the API from the browser
CORS_ORIGIN=http://localhost:5173

# Seeds a demo admin user on boot. Regular users created via the picker have
# no password; the admin account does, so switching into it requires it.
ADMIN_EMAIL=admin@ggi.test
ADMIN_NAME=Admin
ADMIN_PASSWORD=Admin123!
